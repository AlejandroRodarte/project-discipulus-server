const types = require('./types');
const fields = require('../fields');

module.exports = {
    [fields.GRADE]: types.GRADE,
    [fields.POINTS_SUM]: types.POINTS_SUM,
    [fields.USER_NAME]: types.USER,
    [fields.USER_USERNAME]: types.USER,
    NONE: types.NONE
};
